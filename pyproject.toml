[project]
name = "aleyo-backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic[email]",
    "passlib[bcrypt]",
    "pyjwt",
    "pillow",
    "anthropic"
]

[tool.setuptools]
packages = []
