[project]
name = "backend"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic[email]",
    "passlib[bcrypt]",
    "bcrypt",
    "pyjwt",
    "pillow==12.2.0",
    "anthropic"
]

[tool.setuptools]
packages = []
